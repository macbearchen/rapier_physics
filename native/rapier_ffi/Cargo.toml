[package]
name = "rapier_ffi"
version = "0.2.0"
edition = "2021"

[lib]
name = "rapier_ffi"
crate-type = ["staticlib", "cdylib"]   # staticlib for iOS, cdylib for macOS/Linux

[dependencies]
rapier3d = "0.32.0"

[profile.dev.package.rapier3d]
opt-level = 3

[profile.release]
codegen-units = 1
